#include <stdio.h>
#include <stdlib.h>
#include "node.h"

int FIFO_cache_put(int Newpageno, node_t **head_fifo, int cache_capacity)
{
    //To do: Implement the FIFO algorithm
    /*
      Parameter:
      - Newpageno: The page number being assessed
      - head_fifo: The pointer pointing at the header pointer for the fifo cache
      - cache_capacity: The capacity of FIFO cache

      Return:
      int value, indicating whether the page is missed, 1 for missing and 0 for not missing
    */
}