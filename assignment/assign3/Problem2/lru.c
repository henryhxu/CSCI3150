#include <stdio.h>
#include <stdlib.h>
#include "node.h"

int LRU_cache_put(int Newpageno, node_t **head_lru, int cache_capacity)
{
    //To do: Implement the LRU algorithm
    /*
      Parameter:
      - Newpageno: The page number being assessed
      - head_lru: The pointer pointing at the header pointer for the lru cache
      - cache_capacity: The capacity of LRU cache

      Return:
      int value, indicating whether the page is missed, 1 for missing and 0 for not missing
    */

}